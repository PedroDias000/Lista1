#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;
    
    printf("Digite um número inteiro: ");
    scanf("%d", &num);

    int valorabsoluto = abs(num);
    printf("O valor absoluto é: %d\n", valorabsoluto);

    return 0;
}
