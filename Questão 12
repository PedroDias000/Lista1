#include <stdio.h>

int main() {
    float numero1, numero2;

    printf("Digite o primeiro número: ");
    scanf("%f", &numero1);

    printf("Digite o segundo número: ");
    scanf("%f", &numero2);

    printf("Soma: %.2f\n", numero1 + numero2);
    printf("Produto: %.2f\n", numero1 * numero2);
    printf("Diferença: %.2f\n", numero1 - numero2);
    
    if (numero2 != 0) {
        printf("Quociente: %.2f\n", numero1 / numero2);
        printf("Resto: %.2f\n", fmod(numero1, numero2));
    } else {
        printf("Não é possível calcular o quociente
